<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.iu.home.member.MemberMapper">
	
	<select id="getLogin" parameterType="MemberVO" resultType="MemberVO">
		SELECT * FROM MEMBER WHERE ID=#{id} AND PW=#{pw}
	</select>
	
	<insert id="setJoin" parameterType="MemberVO">
		INSERT INTO MEMBER VALUES (#{id},#{pw},#{name},#{email},true)
	</insert>
	
	<insert id="setMemberRole" parameterType="MemberVO">
		INSERT INTO MEMBER_ROLE VALUES (#{id},1)
	</insert>
	
	<select id="getIdCheck" parameterType="MemberVO" resultType="MemberVO">
		SELECT * FROM MEMBER WHERE ID=#{id}
	</select>

</mapper>